{
  "name": "rsidb",
  "version": "0.0.1",
  "description": "A database view for RSI ships.",
  "main": "js/index.js",
  "dependencies": {
    "react": "^0.12.0"
  },
  "devDependencies": {
    "browserify": "^6.2.0",
    "envify": "^3.0.0",
    "jest-cli": "~0.1.17",
    "reactify": "^0.15.2",
    "uglify-js": "~2.4.15",
    "watchify": "^2.1.1",
    "lodash": "^3.5.0"
  },
  "scripts": {
    "start": "watchify js/ -o bundle.js -d -v .",
    "build": "SET NODE_ENV=production browserify . | uglifyjs -cm > bundle.min.js"
  },
  "author": "Kevin Sherman",
  "license": "MIT",
  "browserify": {
    "transform": [
      "reactify",
      "envify"
    ]
  }
}
